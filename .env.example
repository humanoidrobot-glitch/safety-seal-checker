# PostgreSQL credentials (used by both docker-compose and the backend)
# IMPORTANT: Change POSTGRES_PASSWORD to a secure value before first run!
# Then update the password in DATABASE_URL below to match.
POSTGRES_USER=sealcheck
POSTGRES_PASSWORD=changeme
POSTGRES_DB=sealcheck

# Backend database connection string
# When running with Docker Compose, the hostname is "db" (the service name).
# When running locally outside Docker, change "db" to "localhost".
DATABASE_URL=postgresql://sealcheck:changeme@db:5432/sealcheck
